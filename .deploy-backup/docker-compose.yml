# docker-compose-api.yml
# Serviços essenciais para desenvolvimento da API
# Use: docker-compose -f docker-compose-api.yml up
services:
  postgres:
    image: postgres:15.5
    platform: linux/amd64
    container_name: postgres
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - '${POSTGRES_PORT}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  # Redis Cluster é gerenciado pelo docker-compose de storage
  # Os microsserviços se conectam aos nós do cluster via rede keepguard_net

  ms-auth:
    image: ghcr.io/keepguard/ms-auth:1.1.4-SNAPSHOT
    container_name: ms-auth
    pull_policy: always
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8081
    ports:
      - "${MS_AUTH_PORT}:8081"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8081/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  ms-communication:
    image: ghcr.io/keepguard/ms-communication:1.1.7-SNAPSHOT
    container_name: ms-communication
    pull_policy: always
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8082
    ports:
      - "${MS_COMMUNICATION_PORT}:8082"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8082/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  ms-company:
    image: ghcr.io/keepguard/ms-company:1.1.3-SNAPSHOT
    container_name: ms-company
    pull_policy: always
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8083
    ports:
      - "${MS_COMPANY_PORT}:8083"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8083/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  ms-user:
    image: ghcr.io/keepguard/ms-user:1.1.8-SNAPSHOT
    container_name: ms-user
    pull_policy: always
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8085
    ports:
      - "${MS_USER_PORT}:8085"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8085/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  ms-user-consents:
    image: ghcr.io/keepguard/ms-user-consents:1.1.3-SNAPSHOT
    container_name: ms-user-consents
    pull_policy: always
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8086
    ports:
      - "${MS_USER_CONSENTS_PORT}:8086"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8086/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G

  srv-email-google-sender:
    image: ghcr.io/keepguard/srv-email-google-sender:1.0.0
    container_name: srv-email-google-sender
    pull_policy: always
    restart: always
    environment:
      - APP_ENV=dev
    ports:
      - "8601:8601"
    volumes:
      - ../../../backend/srv/srv-email-google-sender/secure:/app/secure:rw
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8601/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  srv-token-manager:
    image: ghcr.io/keepguard/srv-token-manager:1.0.7-SNAPSHOT
    container_name: srv-token-manager
    pull_policy: always
    restart: always
    environment:
      - APP_ENV=dev
    ports:
      - "8700:8700"
    volumes:
      - ../../../backend/srv/srv-token-manager/secure:/app/secure:rw
      - ../../../backend/srv/srv-token-manager/config:/app/config:ro
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8700/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  bff-auth:
    image: ghcr.io/keepguard/bff-auth:1.0.7
    container_name: bff-auth
    pull_policy: always
    restart: always
    environment:
      - APP_ENV=dev
      - CONFIG_FILE=/app/application-dev.yml
      - HEALTHCHECK_URL=http://localhost:8381/health
    ports:
      - "8381:8381"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD", "/app/healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  bff-core:
    image: ghcr.io/keepguard/bff-core:1.0.14
    container_name: bff-core
    pull_policy: always
    restart: always
    environment:
      - APP_ENV=dev
      - CONFIG_FILE=/app/application-dev.yml
      - HEALTHCHECK_URL=http://localhost:8382/health
    ports:
      - "8382:8382"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD", "/app/healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  ms-movie-query:
    image: ghcr.io/keepguard/ms-movie-query:1.0.1
    container_name: ms-movie-query
    pull_policy: never
    restart: always
    environment:
      - ENV=dev
    ports:
      - "${MS_MOVIE_QUERY_PORT}:8350"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:8350/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  bff-movie:
    image: ghcr.io/keepguard/bff-movie:1.0.4
    container_name: bff-movie
    pull_policy: never
    restart: always
    depends_on:
      ms-movie-query:
        condition: service_healthy
    environment:
      - ENV=dev
      - CONFIG_FILE=/app/application-dev.yml
    ports:
      - "${BFF_MOVIE_PORT}:8351"
      - "${BFF_MOVIE_METRICS_PORT}:9093"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:8351/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  srv-ai:
    image: ghcr.io/keepguard/srv-ai:1.0.15
    container_name: srv-ai
    pull_policy: always
    restart: always
    environment:
      - APP_ENV=dev
    ports:
      - "8485:8485"
    volumes:
      - ../../../backend/srv/srv-ai/config:/app/config:ro
      - ../../../backend/srv/srv-ai/prompts:/app/prompts:ro
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8485/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  bff-chat:
    image: ghcr.io/keepguard/bff-chat:1.0.5
    container_name: bff-chat
    pull_policy: always
    restart: always
    depends_on:
      srv-ai:
        condition: service_healthy
    environment:
      - ENV=dev
    ports:
      - "8384:8384"
      - "9091:9091"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8384/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  ms-movie-command:
    image: ghcr.io/keepguard/ms-movie-command:1.0.0
    container_name: ms-movie-command
    pull_policy: always
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    ports:
      - "${MS_MOVIE_COMMAND_PORT}:8089"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8089/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  ms-social:
    image: ghcr.io/keepguard/ms-social:2.1.24
    container_name: ms-social
    pull_policy: always
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    ports:
      - "${MS_SOCIAL_PORT}:8190"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8190/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  bff-social:
    image: ghcr.io/keepguard/bff-social:1.0.22
    container_name: bff-social
    pull_policy: always
    restart: always
    environment:
      - ENV=dev
    ports:
      - "${BFF_SOCIAL_PORT}:8385"
      - "${BFF_SOCIAL_METRICS_PORT}:9094"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:8385/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  ms-user-profile:
    image: ghcr.io/keepguard/ms-user-profile:2.1.3
    container_name: ms-user-profile
    pull_policy: always
    restart: always
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    ports:
      - "${MS_USER_PROFILE_PORT}:8091"
    networks:
      - keepguard_net
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8091/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  # Comentado: Nginx proxy não é mais necessário neste momento
  # Os microserviços serão acessados diretamente via localhost:{porta}
  # nginx-proxy:
  #   image: nginxproxy/nginx-proxy:1.3.0
  #   platform: linux/amd64
  #   container_name: nginx-proxy
  #   restart: always
  #   ports:
  #     - "${NGINX_HTTP_PORT}:80"
  #     - "${NGINX_HTTPS_PORT}:443"
  #   volumes:
  #     - /var/run/docker.sock:/tmp/docker.sock:ro
  #     - ../../nginx/certs:/etc/nginx/certs
  #     - ../../nginx/vhost.d:/etc/nginx/vhost.d
  #     - ../../nginx/html:/usr/share/nginx/html
  #     - ../../nginx/conf.d:/etc/nginx/conf.d
  #   networks:
  #     - proxy
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost/"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 30s
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 256M
  #       reservations:
  #         memory: 128M

  # nginx-proxy-acme:
  #   image: nginxproxy/acme-companion:2.2.0
  #   platform: linux/amd64
  #   container_name: nginx-proxy-acme
  #   restart: always
  #   environment:
  #     - DEFAULT_EMAIL=${LETSENCRYPT_EMAIL}
  #   volumes_from:
  #     - nginx-proxy
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock:ro
  #     - ../../nginx/certs:/etc/nginx/certs:rw
  #     - ../../nginx/acme:/etc/acme.sh
  #   networks:
  #     - proxy
  #   depends_on:
  #     - nginx-proxy:
  #       condition: service_healthy
  #   healthcheck:
  #     test: ["CMD-SHELL", "pgrep -f acme.sh || exit 1"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #       start_period: 30s
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 256M
  #       reservations:
  #         memory: 128M

volumes:
  postgres_data:
  maven_cache:

networks:
  keepguard_net:
    external: true
  # Comentado: Rede proxy não é mais necessária
  # proxy:
  #   external: true 
